import React from 'react';
import '../styles/footerStyle.css'; // Ensure the path is correct

const Footer: React.FC = () => {
    return (
        <footer className="footer">
            <p className="footer-text">Footer</p>
        </footer>
    );
};

export default Footer;
